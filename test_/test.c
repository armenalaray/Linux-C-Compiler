int foo(int a, int b);

int main(void)
{
    int c = foo(1,2);

    int b;
    int d = 2 > b ? b : c;

    int a;
    if(a > 0)
    {
        return a;
    }
    else{
        return 3;
    }

    while(1)
    {
        int ale;
    }

    do
    {
        int pollo;
    } while (a < 4);
    
    int comida;
    for(comida = 4; comida < 4; comida = comida + 1)
    {
        int cama = 6;
    }
    
}

int foo(int a, int b)
{
    return a + b;
}